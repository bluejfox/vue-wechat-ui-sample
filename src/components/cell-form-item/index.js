import CellFormItem from '../cell-form/src/cell-form-item.vue';

/* istanbul ignore next */
CellFormItem.install = (Vue) => {
  Vue.component('se-cell-form-item', CellFormItem);
};

export default CellFormItem;
